import { parseM3U, dedupeChannels } from './core/channel-catalog.js?v=20260920-1021';

const BUILD_ID = '20260920-1920';
const DB_NAME = 'webtv-v2-playlists';
const STORE = 'playlists';
const ACTIVE_KEY = 'webtv_v2_active_playlist';
const MY_ID = '__my_playlist__';
const REGISTRY_URL_KEY = 'webtv_v2_registry_url';
const REGISTRY_TOKEN_KEY = 'webtv_v2_registry_token';
const DEFAULT_WORKER = 'https://source-huntatonisworkersdev.atonis.workers.dev';
const $ = id => document.getElementById(id);

let dbPromise;
let tested = null;
let repoCatalog = [];
let currentCatalog = [];
let startupHydrated = false;

function log(message){const box=$('diagnostic-log');if(!box)return;const stamp=new Date().toLocaleTimeString();box.textContent=`[${stamp}] ${message}\n${box.textContent}`.slice(0,18000);}
function escapeHtml(value=''){return String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));}
function escAttr(value=''){return String(value||'').replace(/"/g,"'");}
function normalize(value=''){return String(value||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9α-ω]+/gi,'-').replace(/^-+|-+$/g,'');}
function uid(){return `pl_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,8)}`;}

function openDb(){
  if(dbPromise)return dbPromise;
  dbPromise=new Promise((resolve,reject)=>{
    const req=indexedDB.open(DB_NAME,1);
    req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains(STORE))db.createObjectStore(STORE,{keyPath:'id'});};
    req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error);
  });
  return dbPromise;
}
async function allSaved(){const db=await openDb();return new Promise((resolve,reject)=>{const req=db.transaction(STORE,'readonly').objectStore(STORE).getAll();req.onsuccess=()=>resolve((req.result||[]).sort((a,b)=>(b.updatedAt||0)-(a.updatedAt||0)));req.onerror=()=>reject(req.error);});}
async function getSaved(id){const db=await openDb();return new Promise((resolve,reject)=>{const req=db.transaction(STORE,'readonly').objectStore(STORE).get(id);req.onsuccess=()=>resolve(req.result||null);req.onerror=()=>reject(req.error);});}
async function putSaved(item){const db=await openDb();return new Promise((resolve,reject)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).put(item);tx.oncomplete=()=>resolve(item);tx.onerror=()=>reject(tx.error);});}
async function removeSaved(id){const db=await openDb();return new Promise((resolve,reject)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).delete(id);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);});}

function activeState(){try{return JSON.parse(localStorage.getItem(ACTIVE_KEY)||'null');}catch{return null;}}
function setActiveState(value){if(value)localStorage.setItem(ACTIVE_KEY,JSON.stringify(value));else localStorage.removeItem(ACTIVE_KEY);}
function registryUrl(){return (localStorage.getItem(REGISTRY_URL_KEY)||'').trim().replace(/\/$/,'');}
function registryToken(){return localStorage.getItem(REGISTRY_TOKEN_KEY)||'';}
function setRegistrySettings(url,token){const clean=String(url||'').trim().replace(/\/$/,'');if(clean)localStorage.setItem(REGISTRY_URL_KEY,clean);else localStorage.removeItem(REGISTRY_URL_KEY);if(token)localStorage.setItem(REGISTRY_TOKEN_KEY,token);else localStorage.removeItem(REGISTRY_TOKEN_KEY);}
function registryHeaders({json=false,auth=false}={}){const headers={};if(json)headers['content-type']='application/json';if(auth&&registryToken())headers.authorization=`Bearer ${registryToken()}`;return headers;}
async function registryFetch(path,options={}){const base=registryUrl();if(!base)throw new Error('Registry URL is not configured');const c=new AbortController();const timer=setTimeout(()=>c.abort(),12000);try{return await fetch(`${base}${path}`,{cache:'no-store',signal:c.signal,...options});}finally{clearTimeout(timer);}}

function setStatus(text,tone='idle'){const el=$('playlist-manager-status');if(!el)return;el.textContent=text;el.dataset.tone=tone;}
function selectedMode(){return document.querySelector('input[name="playlist-mode"]:checked')?.value||'replace';}
function summarize(text){const channels=parseM3U(text);const groups=[...new Set(channels.map(c=>c.group||'Other'))];return{channels,count:channels.length,groups:groups.length,sample:channels.slice(0,8).map(c=>c.name)};}

function channelToLines(channel){const urls=[...new Set((channel.directUrls||[]).filter(u=>/^https?:\/\//i.test(u)))];const ext=`#EXTINF:-1 tvg-id="${escAttr(channel.originalId||channel.id||channel.name)}" tvg-name="${escAttr(channel.name)}" tvg-logo="${escAttr(channel.logo||'')}" group-title="${escAttr(channel.group||'Other')}",${channel.name}`;if(!urls.length)return[ext,''];const lines=[];for(const url of urls)lines.push(ext,url);return lines;}
function channelsToM3U(channels){const lines=['#EXTM3U'];for(const c of channels)lines.push(...channelToLines(c));return `${lines.join('\n')}\n`;}
function channelPayload(channel,position=999999){return{id:normalize(channel.id||channel.originalId||channel.name),name:channel.name,tvgId:channel.originalId||channel.id||channel.name,logo:channel.logo||'',groupName:channel.group||'Other',directUrls:[...(channel.directUrls||[])],sources:(channel.directUrls||[]).map((url,i)=>({url,origin:'playlist',priority:100+i})),position,replaceSources:true};}

async function getMyItem(){const item=await getSaved(MY_ID);if(item)return item;return{id:MY_ID,name:'My Playlist',type:'curated',url:'',text:'#EXTM3U\n',channelCount:0,groupCount:0,createdAt:Date.now(),updatedAt:Date.now()};}
async function saveMyChannels(channels,{sync=true}={}){const text=channelsToM3U(dedupeChannels(channels));const summary=summarize(text),old=await getMyItem();const item={...old,text,channelCount:summary.count,groupCount:summary.groups,updatedAt:Date.now()};await putSaved(item);if(sync&&registryUrl()&&registryToken())await pushMyPlaylistToRegistry(summary.channels).catch(error=>log(`D1 MY PLAYLIST SYNC FAILED · ${error.message}`));await renderMyPlaylist();await renderSaved();await updateMyAction();return item;}

function renderPreview(summary,label='M3U preview'){const box=$('playlist-preview');if(!box)return;box.innerHTML='';const top=document.createElement('div');top.className='playlist-preview-top';top.innerHTML=`<strong>${escapeHtml(label)}</strong><span>${summary.count} channels · ${summary.groups} groups</span>`;box.appendChild(top);const chips=document.createElement('div');chips.className='playlist-preview-chips';for(const name of summary.sample){const chip=document.createElement('span');chip.textContent=name;chips.appendChild(chip);}if(summary.count>summary.sample.length){const more=document.createElement('span');more.textContent=`+${summary.count-summary.sample.length} more`;chips.appendChild(more);}box.appendChild(chips);}
async function fetchWithTimeout(url,timeout=9000){const c=new AbortController();const t=setTimeout(()=>c.abort(),timeout);try{return await fetch(url,{cache:'no-store',signal:c.signal});}finally{clearTimeout(t);}}
async function fetchPlaylist(url){const clean=String(url||'').trim();if(!/^https?:\/\//i.test(clean))throw new Error('Valid http/https playlist URL required');try{const r=await fetchWithTimeout(clean,9000);if(r.ok){const text=await r.text();if(summarize(text).count)return{text,via:'direct'};}}catch{}const proxy=`${DEFAULT_WORKER}/playlist-proxy?url=${encodeURIComponent(clean)}`;const r=await fetchWithTimeout(proxy,12000);if(!r.ok){let detail='';try{detail=(await r.json())?.error||'';}catch{}throw new Error(`Playlist fetch failed${detail?` · ${detail}`:` · HTTP ${r.status}`}`);}const text=await r.text();if(!summarize(text).count)throw new Error('No #EXTINF channels found');return{text,via:'worker'};}

function api(){return window.WebTVPlaylistAPI||null;}
function updateCurrentCatalog(text,mode){const imported=parseM3U(text);currentCatalog=mode==='merge'?dedupeChannels([...currentCatalog,...imported]):dedupeChannels(imported);}
async function applyText(text,mode,label){const bridge=api();if(!bridge)throw new Error('WebTV playlist bridge is not ready');const result=bridge.applyText(text,{mode,label});updateCurrentCatalog(text,mode);setStatus(`${label} loaded · ${result.imported} channels · ${mode}`,'ok');log(`PLAYLIST ${mode.toUpperCase()} · ${label} · ${result.imported} imported · ${result.total} total`);return result;}

async function testUrl(){const url=$('playlist-add-url')?.value.trim();if(!url)return;setStatus('Fetching playlist URL…','busy');try{const fetched=await fetchPlaylist(url);const summary=summarize(fetched.text);tested={text:fetched.text,url,type:'url',summary};renderPreview(summary,`URL test · ${fetched.via}`);setStatus(`Valid M3U · ${summary.count} channels`,'ok');}catch(error){setStatus(error.message,'error');log(`PLAYLIST URL TEST FAILED · ${error.message}`);}}
function testPaste(){const text=$('playlist-paste')?.value||'';const summary=summarize(text);if(!summary.count){setStatus('No #EXTINF channels found in pasted text','error');return;}tested={text,type:'paste',summary};renderPreview(summary,'Pasted M3U test');setStatus(`Valid M3U · ${summary.count} channels`,'ok');}
async function currentPayload(kind){if(kind==='url'){const url=$('playlist-add-url')?.value.trim();if(!url)throw new Error('Playlist URL is empty');if(tested?.type==='url'&&tested.url===url)return tested;const fetched=await fetchPlaylist(url);return{text:fetched.text,url,type:'url',summary:summarize(fetched.text)};}const text=$('playlist-paste')?.value||'';const summary=summarize(text);if(!summary.count)throw new Error('No channels found in pasted M3U');return{text,type:'paste',summary};}
async function loadTemporary(kind){try{setStatus('Preparing temporary playlist…','busy');const p=await currentPayload(kind);const label=kind==='url'?(p.url||'External URL'):'Pasted M3U';await applyText(p.text,selectedMode(),label);renderPreview(p.summary,'Loaded temporarily');}catch(error){setStatus(error.message,'error');}}

async function pushSavedPlaylistToRegistry(item){if(!registryUrl()||!registryToken())return;const r=await registryFetch('/api/playlists',{method:'POST',headers:registryHeaders({json:true,auth:true}),body:JSON.stringify({id:item.id,name:item.name,kind:item.type||'saved',sourceUrl:item.url||'',rawM3u:item.text,channelCount:item.channelCount||0,groupCount:item.groupCount||0})});if(!r.ok){let j={};try{j=await r.json();}catch{}throw new Error(j.error||`Registry HTTP ${r.status}`);}}
async function saveCurrent(kind){try{setStatus('Saving playlist…','busy');const p=await currentPayload(kind);const nameInput=kind==='url'?$('playlist-url-name'):$('playlist-paste-name');const fallback=kind==='url'?new URL(p.url).hostname:`Playlist ${new Date().toLocaleDateString('de-DE')}`;const name=(nameInput?.value||'').trim()||fallback;const now=Date.now();const item={id:uid(),name,type:kind,url:p.url||'',text:p.text,channelCount:p.summary.count,groupCount:p.summary.groups,createdAt:now,updatedAt:now};await putSaved(item);if(nameInput)nameInput.value='';let cloud='local';if(registryUrl()&&registryToken()){await pushSavedPlaylistToRegistry(item);cloud='local + D1';}setStatus(`${name} saved · ${p.summary.count} channels · ${cloud}`,'ok');log(`PLAYLIST SAVED · ${name} · ${p.summary.count} channels · ${cloud}`);await renderSaved();}catch(error){setStatus(error.message,'error');}}

function downloadM3U(item){const blob=new Blob([item.text],{type:'audio/x-mpegurl;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`${item.name.replace(/[^a-z0-9α-ω_-]+/gi,'_')||'playlist'}.m3u`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(a.href),1000);}
async function makeActive(item,mode){setActiveState({id:item.id,mode});await applyText(item.text,mode,item.name);await renderSaved();setStatus(`${item.name} is now active · ${mode}`,'ok');}
async function useRepo(){setActiveState(null);const bridge=api();if(!bridge)return;const result=bridge.resetToRepo();currentCatalog=repoCatalog.map(c=>({...c,directUrls:[...(c.directUrls||[])]}));setStatus(`Repo Playlist active · ${result.total} channels`,'ok');log(`PLAYLIST ACTIVE · Repo Playlist · ${result.total} channels`);await renderSaved();await updateMyAction();}
function tinyButton(label,cls='ghost'){const b=document.createElement('button');b.type='button';b.className=`button ${cls} mini`;b.textContent=label;return b;}

function ensureMyUi(){if($('my-playlist-library'))return;const savedHead=document.querySelector('.saved-playlists-head');if(!savedHead)return;const section=document.createElement('section');section.id='my-playlist-library';section.className='my-playlist-library';section.innerHTML=`<div class="my-playlist-hero"><div class="my-playlist-mark">★</div><div><p class="eyebrow">CURATED</p><h3>My Playlist</h3><p class="muted small">Your own editable channel list. Add a channel from any loaded playlist, edit its sources, or remove it.</p></div><div class="my-playlist-hero-actions"><span id="my-playlist-count" class="freshness-badge">0 channels</span><button id="my-playlist-use" class="button" type="button">Use My Playlist</button><button id="my-playlist-export" class="button ghost" type="button">Export M3U</button></div></div><div id="my-playlist-channels" class="my-playlist-channels"></div><div class="cloud-library"><div class="cloud-library-head"><div><strong>Cloudflare D1 Library</strong><span>Central sync for all your devices + public /playlist.m3u. Startup merges cloud with local so local verified sources are preserved.</span></div><span id="registry-state" class="cloud-state">Not configured</span></div><div class="cloud-library-grid"><input id="registry-url" type="url" placeholder="https://webtv-registry.your-subdomain.workers.dev"><input id="registry-token" type="password" placeholder="Admin token (stored only in this browser)"></div><div class="cloud-library-actions"><button id="registry-save" class="button playlists" type="button">Save settings</button><button id="registry-test" class="button ghost" type="button">Test connection</button><button id="registry-sync-down" class="button ghost" type="button">Pull from D1</button><button id="registry-sync-up" class="button" type="button">Push local → D1</button><a id="registry-m3u-link" class="button ghost" href="#" target="_blank" rel="noopener noreferrer" hidden>Open /playlist.m3u ↗</a></div></div>`;savedHead.parentNode.insertBefore(section,savedHead);$('my-playlist-use')?.addEventListener('click',async()=>{const item=await getMyItem();if(!item.channelCount){setStatus('My Playlist is empty','idle');return;}setActiveState({id:MY_ID,mode:'replace'});await applyText(item.text,'replace','My Playlist');await renderSaved();setStatus('My Playlist active','ok');});$('my-playlist-export')?.addEventListener('click',async()=>downloadM3U(await getMyItem()));$('registry-save')?.addEventListener('click',saveRegistryUi);$('registry-test')?.addEventListener('click',testRegistry);$('registry-sync-down')?.addEventListener('click',()=>pullFromRegistry({quiet:false,mergeLocal:false}));$('registry-sync-up')?.addEventListener('click',pushAllToRegistry);}

async function renderMyPlaylist(){ensureMyUi();const box=$('my-playlist-channels'),count=$('my-playlist-count');if(!box)return;const item=await getMyItem(),channels=parseM3U(item.text);if(count)count.textContent=`${channels.length} channel${channels.length===1?'':'s'}`;box.innerHTML='';if(!channels.length){box.innerHTML='<div class="my-playlist-empty"><strong>Your playlist is empty.</strong><span>Load any playlist, choose a channel, then use “Add to My Playlist”.</span></div>';return;}for(const channel of channels){const row=document.createElement('article');row.className='my-channel-row';const logo=document.createElement('div');logo.className='my-channel-logo';if(channel.logo){const img=document.createElement('img');img.src=channel.logo;img.alt='';logo.append(img);}else logo.textContent='TV';const main=document.createElement('div');main.className='my-channel-main';const title=document.createElement('strong');title.textContent=channel.name;const meta=document.createElement('span');meta.textContent=`${channel.group||'Other'} · ${(channel.directUrls||[]).length} source${(channel.directUrls||[]).length===1?'':'s'}`;main.append(title,meta);const actions=document.createElement('div');actions.className='my-channel-actions';const edit=tinyButton('Edit','ghost');edit.addEventListener('click',()=>editMyChannel(channel));const sources=tinyButton('Sources','ghost');sources.addEventListener('click',()=>showSources(channel));const remove=tinyButton('Remove','danger');remove.addEventListener('click',()=>removeMyChannel(channel));actions.append(edit,sources,remove);row.append(logo,main,actions);box.append(row);}}

function selectedChannel(){const name=$('channel-name')?.textContent?.trim();if(!name||name==='Επίλεξε κανάλι')return null;const group=$('channel-group')?.textContent?.trim();return currentCatalog.find(c=>c.name===name&&(c.group===group||!group))||currentCatalog.find(c=>c.name===name)||null;}
async function myContains(channel){if(!channel)return false;const item=await getMyItem(),channels=parseM3U(item.text),key=normalize(channel.id||channel.originalId||channel.name);return channels.some(c=>normalize(c.id||c.originalId||c.name)===key);}
function ensureMyActionButton(){let b=$('my-playlist-channel-action');if(b)return b;const actions=document.querySelector('.channel-actions');if(!actions)return null;b=document.createElement('button');b.id='my-playlist-channel-action';b.type='button';b.className='button my-add';b.hidden=true;actions.prepend(b);b.addEventListener('click',toggleSelectedInMy);return b;}
async function updateMyAction(){const b=ensureMyActionButton();if(!b)return;const channel=selectedChannel();if(!channel){b.hidden=true;return;}const inside=await myContains(channel);b.hidden=false;b.dataset.inside=inside?'1':'0';b.textContent=inside?'Remove from My Playlist':'★ Add to My Playlist';}
async function toggleSelectedInMy(){const channel=selectedChannel();if(!channel)return;if(await myContains(channel))await removeMyChannel(channel);else await addMyChannel(channel);}
async function addMyChannel(channel){const existing=parseM3U((await getMyItem()).text);let add={...channel,directUrls:[...(channel.directUrls||[])]};if(!add.directUrls.length){const candidate=$('candidate-url')?.value.trim();if(/^https?:\/\//i.test(candidate))add.directUrls=[candidate];}const merged=dedupeChannels([...existing,add]);await saveMyChannels(merged,{sync:false});if(registryUrl()&&registryToken())await putRegistryChannel(add,merged.findIndex(c=>normalize(c.id)===normalize(add.id))).catch(error=>log(`D1 ADD CHANNEL FAILED · ${error.message}`));setStatus(`${channel.name} added to My Playlist`,'ok');log(`MY PLAYLIST ADD · ${channel.name} · ${add.directUrls.length} source(s)`);await updateMyAction();}
async function removeMyChannel(channel){if(!confirm(`Remove “${channel.name}” from My Playlist?`))return;const key=normalize(channel.id||channel.originalId||channel.name),remaining=parseM3U((await getMyItem()).text).filter(c=>normalize(c.id||c.originalId||c.name)!==key);await saveMyChannels(remaining,{sync:false});if(registryUrl()&&registryToken())await deleteRegistryChannel(key).catch(error=>log(`D1 REMOVE CHANNEL FAILED · ${error.message}`));setStatus(`${channel.name} removed from My Playlist`,'idle');log(`MY PLAYLIST REMOVE · ${channel.name}`);await updateMyAction();}
async function editMyChannel(channel){const name=prompt('Channel name',channel.name);if(name===null||!name.trim())return;const group=prompt('Group',channel.group||'Other');if(group===null)return;const urlsText=prompt('Sources, one URL per line',(channel.directUrls||[]).join('\n'));if(urlsText===null)return;const urls=urlsText.split(/\r?\n|,/).map(s=>s.trim()).filter(s=>/^https?:\/\//i.test(s));const key=normalize(channel.id||channel.originalId||channel.name),all=parseM3U((await getMyItem()).text);const index=all.findIndex(c=>normalize(c.id||c.originalId||c.name)===key);if(index<0)return;const edited={...all[index],name:name.trim(),group:(group||'Other').trim(),directUrls:[...new Set(urls)]};all[index]=edited;await saveMyChannels(all,{sync:false});if(registryUrl()&&registryToken())await putRegistryChannel(edited,index,true).catch(error=>log(`D1 EDIT CHANNEL FAILED · ${error.message}`));setStatus(`${edited.name} updated`,'ok');await updateMyAction();}
function showSources(channel){const urls=(channel.directUrls||[]);alert(urls.length?`${channel.name}\n\n${urls.map((u,i)=>`${i+1}. ${u}`).join('\n\n')}`:`${channel.name}\n\nNo stream source stored.`);}

async function saveRegistryUi(){const url=$('registry-url')?.value||'',token=$('registry-token')?.value||'';setRegistrySettings(url,token);updateRegistryUi();setStatus(registryUrl()?'D1 settings saved':'D1 settings cleared','ok');if(registryUrl())await pullFromRegistry({quiet:true,mergeLocal:true}).catch(()=>{});}
function updateRegistryUi(){ensureMyUi();const url=registryUrl(),token=registryToken();if($('registry-url'))$('registry-url').value=url;if($('registry-token'))$('registry-token').value=token;const state=$('registry-state');if(state){state.textContent=url?(token?'Configured':'Read only'):'Not configured';state.dataset.online='0';}const link=$('registry-m3u-link');if(link){link.hidden=!url;link.href=url?`${url}/playlist.m3u`:'#';}}
async function testRegistry(){const state=$('registry-state');if(state)state.textContent='Testing…';try{const r=await registryFetch('/api/status');const j=await r.json();if(!r.ok)throw new Error(j.error||`HTTP ${r.status}`);if(state){state.textContent=`D1 online · ${j.myPlaylistChannels||0} channels`;state.dataset.online='1';}setStatus(`Registry ${j.version||''} connected`,'ok');}catch(error){if(state){state.textContent='Connection failed';state.dataset.online='0';}setStatus(`D1: ${error.message}`,'error');}}
async function putRegistryChannel(channel,position=999999,replaceSources=false){const r=await registryFetch('/api/my-playlist/channel',{method:'PUT',headers:registryHeaders({json:true,auth:true}),body:JSON.stringify({...channelPayload(channel,position),replaceSources})});if(!r.ok){let j={};try{j=await r.json();}catch{}throw new Error(j.error||`Registry HTTP ${r.status}`);}return r.json();}
async function deleteRegistryChannel(id){const r=await registryFetch(`/api/my-playlist/channel/${encodeURIComponent(normalize(id))}`,{method:'DELETE',headers:registryHeaders({auth:true})});if(!r.ok){let j={};try{j=await r.json();}catch{}throw new Error(j.error||`Registry HTTP ${r.status}`);}}
async function pushMyPlaylistToRegistry(channels){const payload={channels:channels.map((c,i)=>channelPayload(c,i))};const r=await registryFetch('/api/my-playlist/replace',{method:'POST',headers:registryHeaders({json:true,auth:true}),body:JSON.stringify(payload)});if(!r.ok){let j={};try{j=await r.json();}catch{}throw new Error(j.error||`Registry HTTP ${r.status}`);}}
async function pushAllToRegistry(){try{if(!registryUrl()||!registryToken())throw new Error('Registry URL and admin token are required');setStatus('Pushing local library to D1…','busy');const my=parseM3U((await getMyItem()).text);await pushMyPlaylistToRegistry(my);const rows=(await allSaved()).filter(x=>x.id!==MY_ID);for(const item of rows)await pushSavedPlaylistToRegistry(item);await testRegistry();setStatus(`D1 updated · ${my.length} My Playlist channels · ${rows.length} saved playlists`,'ok');log(`D1 PUSH COMPLETE · ${my.length} channels · ${rows.length} playlists`);}catch(error){setStatus(`D1 push failed · ${error.message}`,'error');}}

async function pullFromRegistry({quiet=false,mergeLocal=false}={}){
  try{
    if(!registryUrl())throw new Error('Registry URL is required');if(!quiet)setStatus('Pulling central library from D1…','busy');
    const localChannels=mergeLocal?parseM3U((await getMyItem()).text):[];
    const myRes=await registryFetch('/api/my-playlist');const myJson=await myRes.json();if(!myRes.ok)throw new Error(myJson.error||`HTTP ${myRes.status}`);
    const remoteChannels=(myJson.channels||[]).map(c=>({id:normalize(c.id||c.tvgId||c.name),originalId:c.tvgId||c.id||c.name,name:c.name,logo:c.logo||'',group:c.groupName||'Other',directUrls:(c.sources||[]).map(s=>s.url).filter(Boolean)}));
    const mergedChannels=mergeLocal?dedupeChannels([...localChannels,...remoteChannels]):remoteChannels;
    await saveMyChannels(mergedChannels,{sync:false});
    let pulledPlaylists=0;
    if(registryToken()){
      const listRes=await registryFetch('/api/playlists');const listJson=await listRes.json();
      if(listRes.ok){
        for(const meta of listJson.playlists||[]){
          const detailRes=await registryFetch(`/api/playlists/${encodeURIComponent(meta.id)}`,{headers:registryHeaders({auth:true})});if(!detailRes.ok)continue;
          const detail=(await detailRes.json()).playlist;if(!detail?.rawM3u)continue;
          const s=summarize(detail.rawM3u);await putSaved({id:detail.id,name:detail.name,type:detail.kind||'saved',url:detail.sourceUrl||'',text:detail.rawM3u,channelCount:detail.channelCount||s.count,groupCount:detail.groupCount||s.groups,createdAt:Date.parse(detail.createdAt)||Date.now(),updatedAt:Date.parse(detail.updatedAt)||Date.now()});pulledPlaylists++;
        }
      }
    }
    await renderSaved();await renderMyPlaylist();await testRegistry();
    if(!quiet)setStatus(`D1 pull complete · ${remoteChannels.length} My Playlist channels · ${pulledPlaylists} saved playlists`,'ok');
    const mergeText=mergeLocal?` · merged with local → ${mergedChannels.length} channels`:'';
    log(`D1 PULL COMPLETE · ${remoteChannels.length} remote channels${mergeText} · ${pulledPlaylists} saved playlists${quiet?' · startup':''}`);
    return {channels:mergedChannels.length,remoteChannels:remoteChannels.length,playlists:pulledPlaylists,merged:mergeLocal};
  }catch(error){if(!quiet)setStatus(`D1 pull failed · ${error.message}`,'error');log(`D1 PULL FAILED${quiet?' at startup':''} · ${error.message}`);throw error;}
}

async function renderSaved(){const box=$('saved-playlists'),count=$('saved-playlist-count');if(!box)return;const rows=(await allSaved()).filter(x=>x.id!==MY_ID),active=activeState();if(count)count.textContent=`${rows.length} saved`;box.innerHTML='';const repo=document.createElement('article');repo.className=`playlist-card builtin${!active?' active':''}`;repo.innerHTML=`<div class="playlist-card-icon">⌂</div><div class="playlist-card-main"><div class="playlist-card-title"><strong>Repo Playlist</strong>${!active?'<span class="playlist-active-badge">ACTIVE</span>':''}</div><span>Built-in fallback · GitHub repository</span></div>`;const repoActions=document.createElement('div');repoActions.className='playlist-card-actions';const repoLoad=tinyButton('Use','ghost');repoLoad.addEventListener('click',useRepo);repoActions.append(repoLoad);repo.append(repoActions);box.appendChild(repo);for(const item of rows){const isActive=active?.id===item.id,card=document.createElement('article');card.className=`playlist-card${isActive?' active':''}`;const icon=document.createElement('div');icon.className='playlist-card-icon';icon.textContent=item.type==='url'?'↗':'≡';const main=document.createElement('div');main.className='playlist-card-main';const title=document.createElement('div');title.className='playlist-card-title';const strong=document.createElement('strong');strong.textContent=item.name;title.appendChild(strong);if(isActive){const badge=document.createElement('span');badge.className='playlist-active-badge';badge.textContent=`ACTIVE · ${active.mode}`;title.appendChild(badge);}const meta=document.createElement('span');let host='';try{host=item.url?new URL(item.url).hostname:'';}catch{}meta.textContent=`${item.channelCount||0} channels · ${item.groupCount||0} groups${host?` · ${host}`:''}`;main.append(title,meta);const actions=document.createElement('div');actions.className='playlist-card-actions';const load=tinyButton('Load');load.addEventListener('click',()=>applyText(item.text,selectedMode(),item.name).catch(e=>setStatus(e.message,'error')));const activeBtn=tinyButton('Set Active','ghost');activeBtn.addEventListener('click',()=>makeActive(item,selectedMode()));const rename=tinyButton('Rename','ghost');rename.addEventListener('click',async()=>{const name=prompt('Playlist name',item.name);if(!name?.trim())return;const changed={...item,name:name.trim(),updatedAt:Date.now()};await putSaved(changed);if(registryUrl()&&registryToken())await pushSavedPlaylistToRegistry(changed).catch(()=>{});await renderSaved();});const exportBtn=tinyButton('Export','ghost');exportBtn.addEventListener('click',()=>downloadM3U(item));const del=tinyButton('Delete','danger');del.addEventListener('click',async()=>{if(!confirm(`Delete “${item.name}”?`))return;await removeSaved(item.id);if(registryUrl()&&registryToken())registryFetch(`/api/playlists/${encodeURIComponent(item.id)}`,{method:'DELETE',headers:registryHeaders({auth:true})}).catch(()=>{});if(active?.id===item.id){setActiveState(null);api()?.resetToRepo();currentCatalog=repoCatalog.map(c=>({...c,directUrls:[...(c.directUrls||[])]}));}await renderSaved();setStatus(`${item.name} deleted`,'idle');});actions.append(load,activeBtn,rename,exportBtn,del);card.append(icon,main,actions);box.appendChild(card);}}

async function restoreActive(){const active=activeState();if(!active)return;const item=await getSaved(active.id);if(!item){setActiveState(null);return;}try{await applyText(item.text,active.mode||'replace',`${item.name} · startup`);}catch(error){setStatus(`Active playlist failed · ${error.message}`,'error');}}
async function loadRepoCatalog(){try{const r=await fetch('./data/channels.m3u',{cache:'no-store'});if(r.ok){repoCatalog=parseM3U(await r.text());if(!currentCatalog.length)currentCatalog=repoCatalog.map(c=>({...c,directUrls:[...(c.directUrls||[])]}));}}catch(error){log(`PLAYLIST MANAGER repo catalog unavailable · ${error.message}`);}}

async function startupRestore(){
  if(startupHydrated)return;startupHydrated=true;
  await loadRepoCatalog();
  if(registryUrl()){
    try{await pullFromRegistry({quiet:true,mergeLocal:true});}
    catch{log('D1 startup restore skipped; using local library');}
  }
  await restoreActive();
  await Promise.all([renderSaved(),renderMyPlaylist(),updateMyAction()]);
}

function bind(){$('playlist-manager-toggle')?.addEventListener('click',()=>{const p=$('playlist-manager');if(p){p.hidden=!p.hidden;if(!p.hidden){renderMyPlaylist();renderSaved();updateRegistryUi();}}});$('playlist-manager-close')?.addEventListener('click',()=>{const p=$('playlist-manager');if(p)p.hidden=true;});$('playlist-test-url')?.addEventListener('click',testUrl);$('playlist-load-url')?.addEventListener('click',()=>loadTemporary('url'));$('playlist-save-url')?.addEventListener('click',()=>saveCurrent('url'));$('playlist-test-paste')?.addEventListener('click',testPaste);$('playlist-load-paste')?.addEventListener('click',()=>loadTemporary('paste'));$('playlist-save-paste')?.addEventListener('click',()=>saveCurrent('paste'));$('playlist-use-repo')?.addEventListener('click',useRepo);$('playlist-add-url')?.addEventListener('keydown',e=>{if(e.key==='Enter')testUrl();});$('channel-list')?.addEventListener('click',()=>setTimeout(updateMyAction,0));const observer=new MutationObserver(()=>updateMyAction());const name=$('channel-name');if(name)observer.observe(name,{childList:true,characterData:true,subtree:true});}

window.WebTVMyPlaylistAPI={addCurrent:()=>{const c=selectedChannel();return c?addMyChannel(c):Promise.reject(new Error('No channel selected'));},addSourceToCurrent:async(url)=>{const c=selectedChannel();if(!c)throw new Error('No channel selected');const existing=parseM3U((await getMyItem()).text);const key=normalize(c.id||c.originalId||c.name);let target=existing.find(x=>normalize(x.id||x.originalId||x.name)===key);if(!target){target={...c,directUrls:[...(c.directUrls||[])]};existing.push(target);}target.directUrls=[...new Set([...(target.directUrls||[]),url].filter(Boolean))];await saveMyChannels(existing,{sync:false});if(registryUrl()&&registryToken())await putRegistryChannel(target,existing.indexOf(target),true);return target;},getMyPlaylist:async()=>parseM3U((await getMyItem()).text)};

bind();
ensureMyUi();updateRegistryUi();
renderSaved().catch(error=>setStatus(`Playlist DB error · ${error.message}`,'error'));
renderMyPlaylist().catch(error=>setStatus(`My Playlist error · ${error.message}`,'error'));
window.addEventListener('webtv:ready',()=>startupRestore().catch(error=>setStatus(`Startup restore failed · ${error.message}`,'error')),{once:true});
if(window.WebTVPlaylistAPI?.ready)startupRestore().catch(()=>{});
log(`Playlist Manager loaded · build ${BUILD_ID} · D1 startup merge preserves local My Playlist`);